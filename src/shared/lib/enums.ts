export enum SupportedTheme {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum ColorType {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  GHOST = 'ghost',
  LINK = 'link',
  DEFAULT = 'default',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
}

export enum SizeType {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export enum WeightType {
  THIN = 100,
  EXTRA_LIGHT = 200,
  LIGHT = 300,
  REGULAR = 400,
  MEDIUM = 500,
  SEMI_BOLD = 600,
  BOLD = 700,
  EXTRA_BOLD = 800,
  BLACK = 900,
}
